import { FC } from 'react';
import Wrapper from './Wrapper';
import Header from './Header';

const Layout: FC = () => {
  return (
    <Wrapper>
      <Header />
      <main>dfd</main>
    </Wrapper>
  );
};

export default Layout;
