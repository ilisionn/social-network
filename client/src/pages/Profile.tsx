import { FC } from 'react';
import ProfileHeader from 'src/components/ProfileHeader';

const Profile: FC = () => {
  return (
    <div>
      <ProfileHeader />
    </div>
  );
};

export default Profile;
